def notas(*n, sit=False):
    global dados
    dados = dict()
    dados = {'total':len(n)}
    dados = {'maior':max(n)}
    dados = {'menor':min(n)}
    for c in range(0, len(n)):

        m += n
        m /= len(n)
    dados = ['média'] = round(m, 1)
    if sit:
        if
        elif
        else
    else:
        return dados

notas(5, 2, 3)